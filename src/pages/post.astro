---
const response = await fetch("https://jsonplaceholder.typicode.com/posts")
import Layout from "../components/layout.astro"
const posts = await response.json()
//console.log(data)
---
<Layout pageTitle="este es el post">
    <style>
    body{
        color: red;
    }

    .true{
        font-size: 50px;
        align-content: center;
        align-items: center;
        text-align: center;
    }

    .holi{
        font-size: 50px;
        text-align: center;
    }
</style>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1 class="true">publicaciones 📱📲</h1>
        {
            posts.map(name =>
             (<div class="holi">
                    <h3>{name.userId}</h3>
                    <h4>{name.id}</h4>
                    <p>{name.title}</p>
                    <p>{name.body}</p>
                </divcl>
            ))
        }
</body>
</html>
</Layout>
